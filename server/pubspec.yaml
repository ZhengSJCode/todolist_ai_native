name: todolist_server
description: Dart REST backend for todolist_ai_native.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  google_generative_ai: ^0.4.7
  http: ^1.3.0
  http_parser: ^4.1.2
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  uuid: ^4.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
